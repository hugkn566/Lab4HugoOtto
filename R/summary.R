#' Summary method for linreg objects
#' 
#' @description \code{summary} method for objects of the class .
#' @param object an object of class "linreg", generated by a call to \code{\link{linreg}}.
#' @param x an object of class "summary.linreg", generated by a call to \code{summary.linreg}.
#' @return A list of summary statistics from a linear regression model, \code{object}, of the class "linreg".
#' @examples 

summary.linreg <- function(object,...){
  m <- matrix(0, nrow = nrow(test$var_beta_hat), ncol = 4, dimnames = list(row.names(test$var_beta_hat), c("Estimate", "Std. Error", "t value", "Pr(>|t|)")))
  m[,1] <- test$beta_hat
  m[,2] <- sqrt(diag(test$var_beta_hat))
  m[,3] <- test$t_beta
  m[,4] <- test$p
  
  res <- paste("Residual standard error:", round(sqrt(test$sigma2_hat), digits = 4), "on", test$df, "degrees of freedom")
  
  list <- list(m,res)
  class(list) <- "summary.linreg"
  return(list)
}

#' @describeIn summary.linreg 

print.summary.linreg <- function(x,...){
  print(x[[1]])
  cat(" ", sep = "\n")
  cat(x[[2]]) 
}

